# BTRemote 蓝牙远程控制工具

## 项目概述

一款手机蓝牙远程控制工具，支持以下功能：
- 手机A（主机）通过蓝牙控制手机B（备机）的网络（WiFi/热点/移动数据）
- 手机B收到短信同步到手机A显示
- 手机A的媒体控制同步到手机B的系统媒体空间（支持手表控制）

## 设备要求

| 设备 | 要求 |
|------|------|
| 手机A（主机） | Android 14+，无root |
| 手机B（备机） | Android 13+，需要root和LSPosed |

## 目录结构

```
安卓工具/
├── tools/                    # 工具目录
│   ├── gradle-8.2/           # Gradle 8.2
│   └── sdk/                  # Android SDK
├── app/                      # 主应用模块
│   ├── src/main/
│   │   ├── java/com/tool/btremote/
│   │   │   ├── bluetooth/    # 蓝牙连接管理
│   │   │   ├── network/      # 网络控制（热点/WiFi/移动数据）
│   │   │   ├── sms/          # 短信同步
│   │   │   ├── media/        # 媒体控制
│   │   │   ├── service/      # 后台服务
│   │   │   ├── keepalive/    # 保活策略
│   │   │   ├── lsposed/      # LSPosed模块（热点控制）
│   │   │   ├── ui/           # 界面Activity
│   │   │   └── App.kt        # 应用入口
│   │   ├── res/              # 资源文件
│   │   ├── AndroidManifest.xml
│   │   └── assets/xposed_init  # LSPosed模块入口
│   └── build.gradle.kts      # 应用构建配置
├── settings.gradle.kts       # 项目设置
├── gradle.properties         # Gradle属性
└── local.properties          # 本地配置（SDK路径）
```

## 核心功能模块

### 1. 蓝牙连接
- 使用SPP协议（UUID: 00001101-0000-1000-8000-00805F9B34FB）
- 心跳保活（15秒间隔，30秒超时）
- 自动重连机制

### 2. 网络控制
- WiFi开关：`svc wifi enable/disable`（需root）
- 移动数据开关：`svc data enable/disable`（需root）
- 热点开关：
  - 优先：反射调用 `TetheringManager.startTethering()`（需LSPosed）
  - 备用：`cmd connectivity start-tethering wlan0`（需root）
  - 备用：`service call connectivity 30`（需root）

### 3. 短信同步
- 手机B监听 `SMS_RECEIVED` 广播
- 通过蓝牙发送短信内容到手机A
- 手机A弹出系统级短信弹窗和通知

### 4. 媒体控制
- 手机A使用 `NotificationListenerService` 监听系统媒体会话
- 手机B创建 `MediaSession` 供系统/手表控制
- 控制命令（播放/暂停/下一曲/音量）通过蓝牙传输

### 5. 后台保活
- 前台服务（`connectedDevice|mediaPlayback`）
- WakeLock 和 WiFiLock
- 开机自启动（`BOOT_COMPLETED`）
- 电池优化白名单

## 编译环境

### Windows 配置步骤

1. **映射盘符解决中文路径问题**（必须）：
```powershell
subst Z: "C:\Users\Administrator\Desktop\安卓工具"
cd Z:\
```

2. **设置环境变量**：
```powershell
$env:ANDROID_HOME = "C:\android-sdk"          # 或 tools/sdk 路径
$env:ANDROID_SDK_ROOT = "C:\android-sdk"
$env:JAVA_HOME = "C:\android-sdk\jdk-17.0.2"  # SDK自带的JDK
```

3. **编译命令**：
```powershell
# 调试版APK
tools\gradle-8.2\bin\gradle.bat assembleDebug

# 发布版APK
tools\gradle-8.2\bin\gradle.bat assembleRelease
```

4. **编译结果**：
```
app/build/outputs/apk/debug/app-debug.apk
app/build/outputs/apk/release/app-release.apk
```

## 安装与使用

### 首次设置

**两台手机都需要：**
1. 打开蓝牙，提前配对
2. 安装APK，授予所有权限

**手机B（备机）额外设置：**
1. 授予root权限
2. 打开LSPosed管理器，启用BTRemote模块，作用范围选择本应用
3. 重启手机
4. 打开APP，选择「从机模式」，开启后台保活和开机自启动
5. 选择要连接的主机设备

**手机A（主机）额外设置：**
1. 打开APP，选择「主机模式」
2. 授予通知读取权限（设置→应用→特殊访问→通知使用权）
3. 选择要连接的从机设备

### 功能说明

| 功能 | 说明 |
|------|------|
| 蓝牙连接 | SPP协议，自动重连，心跳保活 |
| WiFi控制 | 远程开关手机B的WiFi（需root） |
| 热点控制 | 远程开关手机B的热点（需root+LSPosed） |
| 移动数据 | 远程开关手机B的移动数据（需root） |
| 短信同步 | B收到短信后同步到A，A弹窗+通知+铃声 |
| 媒体同步 | A的媒体控制同步到B的系统媒体，支持手表控制 |

## LSPosed模块配置

本应用同时也是一个LSPosed模块，用于绕过热点控制的系统权限限制。

**模块配置步骤：**
1. 打开LSPosed管理器
2. 找到「BTRemote」模块
3. 勾选启用
4. 勾选作用范围：`com.tool.btremote`
5. 重启手机或Zygote

**Hook内容：**
- `TetheringManager.startTethering/stopTethering`
- `ConnectivityManager.getTetheringManager`
- `WifiManager.isWifiApEnabled/setWifiApEnabled`

## 常见问题

### 1. 编译报错「中文路径」
**原因**：项目路径包含中文导致Gradle编译失败
**解决**：使用 `subst` 映射盘符
```powershell
subst Z: "C:\Users\Administrator\Desktop\安卓工具"
cd Z:\
```

### 2. 热点无法开启
**原因**：Android 13+热点API需要系统签名权限
**解决**：
1. 确保手机B已安装LSPosed并启用BTRemote模块
2. 确保手机B已获取root权限
3. 尝试手动执行命令测试：
```bash
su -c "cmd connectivity start-tethering wlan0"
su -c "service call connectivity 30 i32 1 s16 wlan0"
```

### 3. 短信不同步
**原因**：权限未授予或广播未收到
**解决**：
1. 检查手机B的「短信权限」是否已授予
2. 检查手机B的短信应用是否拦截了广播
3. 部分国产ROM需要在「短信应用设置」中允许其他应用读取短信

### 4. 后台服务被杀死
**原因**：手机厂商的电池优化策略
**解决**：
1. 打开APP的「后台保活」功能
2. 在手机设置中关闭对BTRemote的电池优化
3. 将BTRemote加入白名单

## 技术栈

| 技术 | 版本 |
|------|------|
| Kotlin | 1.9.x |
| Android SDK | 34 |
| Gradle | 8.2 |
| Material Design | 1.11.0 |
| Coroutines | 1.7.3 |
| LSPosed | API 82 |

## 注意事项

1. 手机B必须保持root状态和LSPosed激活状态
2. 首次使用需要在两台手机上手动配对蓝牙
3. 热点控制需要LSPosed模块启用才能稳定工作
4. 建议关闭手机B的省电模式以确保后台服务正常运行
5. 手机B锁屏状态下部分功能依赖前台服务保活

## 项目迁移

换电脑后继续开发的步骤：

1. **复制整个项目目录**到新电脑
2. **安装Java 17**（或使用SDK自带的JDK）
3. **配置环境变量**：
```powershell
$env:ANDROID_HOME = "C:\android-sdk"
$env:JAVA_HOME = "C:\android-sdk\jdk-17.0.2"
```
4. **映射盘符**（如果路径有中文）：
```powershell
subst Z: "C:\path\to\安卓工具"
cd Z:\
```
5. **编译**：
```powershell
tools\gradle-8.2\bin\gradle.bat assembleDebug
```

## 开发日志

- v1.0：基础功能完成（蓝牙连接、网络控制、短信同步、媒体控制）
- 热点控制：尝试了多种方式（service call、cmd命令、ndc、TetheringManager反射），最终推荐使用LSPosed模块配合反射调用
